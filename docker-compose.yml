services:
  ubuntu24:
    platform: linux/amd64
    build: .
    container_name: ubuntu24-dev
    tty: true
    stdin_open: true
    ports:
      - 8787:8787
    volumes:
      - ./project:/home/rstudio/project
